# spring-boot-application-started
This is a basic Spring Boot application created to understand the complete setup and startup process. It includes project initialization, dependency configuration, and application execution using Java, Spring Boot and Maven. The project is managed using GitHub for version control.
